rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{userId}/{allPaths=**} {
      allow read: if true;                                   // imagens/vídeos públicos p/ exibir
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
